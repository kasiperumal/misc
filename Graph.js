    useEffect(() => {
        // Call handleSearch when the component mounts
        handleSearch();
    }, []); // Empty dependency array ensures this runs once on mount

  const handleSearch = () => {
    // e.preventDefault();
